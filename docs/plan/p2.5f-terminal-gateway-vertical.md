# P2.5f Terminal and Gateway Vertical Plan

Status: P2.5f0 authority, data-plane, recovery, and deployment audit complete
as design evidence on 2026-08-27. P2.5f1 reconnectable terminal runtime commit
`6778d3c`, evidence baseline `66dd3d1`, and repository CI `33033284420` pass its
local and CI gates. P2.5f2 durable session coordination commit `ccffd52`,
evidence baseline `3abefd4`, and repository CI `33045725476` pass its local and
CI gates. P2.5f3 opaque-reference implementation `b1acdd1`, evidence baseline
`7138a4c`, and repository CI `33059304542` pass its local and CI gates. P2.5f4
WebSocket/terminal adapter implementation `14f14cc` passes local component and
full regression gates; evidence baseline `1d9da67` and repository CI
`33064864447` pass all three jobs. P2.5f5 Gateway composition implementation
`fdfc771`, evidence baseline `754c57d`, and repository CI `33067526022` pass
its local and CI gates. P2.5f6 command composition implementation `c4c7cbc`
with startup/locking regression `8a794c0` has local evidence; its CI evidence
is pending. No public Gateway command composition or nonempty capability
advertisement is enabled.

## Objective

Compose the existing terminal-session authority and Gateway components into a
real, restart-aware development vertical without exposing backend endpoints or
taking ownership of caller users, tenants, public authorization, or reconnect
policy.

P2.5f must make these two boundaries work together while keeping them
independent:

- the Provider asynchronously allocates and owns a provider-local terminal
  resource, then returns only an expiring `ref:session:*` handoff; and
- the calling platform Runtime Gateway authorizes a user, resolves that opaque
  reference over a trusted channel, and proxies the terminal stream.

This plan does not change the repository-owned Provider Contract. Contract,
Gateway, deployment, and external-caller evidence remain separate gates.

## Authority and Protocol Boundary

The locked Contract authorizes only:

- `POST /v1/sandboxes/{sandbox_id}/runtime-sessions`; and
- `GET /v1/operations/{operation_id}/runtime-session`.

The handoff schema identifies the protocol as `websocket`, but it does not
define a public Gateway route, an end-user credential, terminal resize, an
explicit close-session mutation, or a browser/desktop/port-forward protocol.
Those features must not be inferred from local interfaces.

The Provider may persist only provider-local allocation and recovery evidence.
The calling platform remains authoritative for caller identity, tenant policy,
Gateway grants, public routing, revocation policy, and recording policy. No
default or static allow-all authorizer is acceptable for an enabled Gateway.

## Verified Starting Point

| Area | Verified implementation fact | Missing evidence or behavior |
| --- | --- | --- |
| Provider transport | Protected open and handoff routes exist and enforce strict preflight and opaque projection | The command root injects no session application |
| Session application | The P2.5f2 `Vertical` durably reserves, allocates or reconciles one immutable terminal identity, persists bounded observations, retries identity-bound expiry cleanup, and accepts later registry evidence for success | It is not command-composed and does not mint or resolve `ref:session:*` |
| Session repository v2 | File and memory adapters persist sessions, idempotency, synchronized lifecycle projections, immutable allocation receipts, and observations; valid v1 accepted/handoff evidence migrates without redispatching legacy running state | File persistence remains single-controller and separate from lifecycle storage; cross-repository atomicity is not claimed |
| Gateway | Injected `Authorizer`, `ReferenceResolver`, `RevocationSource`, and `Recorder` support bounded reconnect, expiry, revocation interruption, bidirectional frames, and metadata-only audit; P2.5f4 supplies uncomposed WebSocket and terminal wire adapters | No Gateway composition, deployable composition, or caller-owned production ports |
| Terminal runtime | P2.5f1 adds a backend-neutral runtime and a Docker adapter backed by a PTY-owning guest broker; P2.5f2 connects it to the uncomposed session vertical and passes local fault/restart/migration gates; P2.5f3 supplies the opaque resolver and f4 supplies stream adapters | No Gateway composition, resize, or production broker packaging evidence |
| Command/config | Lifecycle and exec can be composed for one development controller | No session, reference registry, Gateway, or terminal configuration |
| Dependencies | `github.com/coder/websocket@v1.8.15` is a direct ISC-licensed dependency with source/license/behavior review; the adapters set explicit limits | Vulnerability scanning was not available; deployment review remains open |

These facts are component and source evidence. They do not establish a
terminal vertical, external caller E2E, tenancy, deployment, or production
readiness.

## Rejected Shortcut: Reusing Docker Exec Attach

The P2.5e executor is not a terminal transport. It creates a Docker exec with
`TTY: false`, does not attach stdin, and retains only a process-local hijacked
stdout/stderr reader while capture is active.

Docker exec start/attach is a one-shot stream lifecycle. The current adapter
cannot reconstruct its lost stream after the Provider process restarts, and a
new shell process would be a different terminal rather than a reconnection to
the accepted runtime session. Persisting the backend exec ID does not make the
stream reconnectable.

Therefore P2.5f must first introduce a genuinely reconnectable terminal
mechanism, such as a guest-side terminal broker/session manager or an
equivalent design with independently attachable state. A test fake, a retained
Go closure, or a newly created shell on every reconnect cannot satisfy the
restart/reconnect gate.

## Data-Plane Decision

The implementation must use focused ports with inward-pointing dependencies:

```text
Provider session transport
          |
          v
durable session coordinator
          |
          +--> terminal runtime port --> Docker terminal adapter --> guest broker
          |
          +--> opaque reference registry

caller-owned WebSocket adapter --> Gateway --> reference resolver --> fresh broker dial
                                  |  |  |
                                  |  |  +--> caller-owned audit recorder
                                  |  +-----> caller-owned revocation source
                                  +--------> caller-owned authorizer
```

The terminal runtime port owns allocation, observation, attachment, and cleanup
of a terminal resource. It returns backend-neutral allocation evidence to the
session coordinator. Backend container IDs, broker socket paths, network
addresses, credentials, and transport handles remain private to the Docker
adapter.

The opaque reference registry binds one `ref:session:*` value to the exact
sandbox, runtime-session, connection generation, expiry, and private adapter
identity. A resolver validates those fields on every connect and reconnect,
then constructs a fresh dial operation. A Go closure is runtime state, not
durable state, and must never be serialized.

## Persistence and Recovery

P2.5f2 introduces a backward-compatible session repository migration. The
provider-neutral session record adds:

- an immutable allocation receipt;
- connection generation without a Contract-visible opaque session reference;
- allocation and observation timestamps; and
- expiry and cleanup state needed for deterministic recovery.

P2.5f3 separately owns the `ref:session:*` record and resolver. The
`ref:terminal/*` allocation receipt persisted by P2.5f2 is not a handoff.

The Docker adapter owns a separate private state record containing the backend
terminal/broker identity needed to observe and reattach. Provider API DTOs and
the session authority repository must not contain that private identity.

The coordinator follows durable accept-before-allocation:

1. Validate Contract and protected admission before application dispatch.
2. Atomically reserve the accepted session request and sandbox authority.
3. Allocate once or reconcile an existing private receipt.
4. Persist running state and the exact allocation receipt.
5. Register the opaque reference and connection generation.
6. Recheck sandbox authority transactionally before committing success and
   minting the handoff.

On startup, accepted and running records are reconciled against private adapter
state. Known absence may become a bounded terminal failure. Uncertain outcome
must remain `outcome_unknown`; it must not allocate a replacement terminal or
mint a handoff. A succeeded, unexpired session is reconnectable only when the
same broker identity can be reconstructed and observed.

Session authority must be derived from trusted lifecycle application state.
Tests may use direct repository setup, but command composition must not depend
on operators or handlers manually calling `PutSandboxAuthority`.

## Lifetime, Cleanup, and Capacity

The current Contract has no close-session route. Gateway grant revocation
interrupts the authorized proxy connection; it does not by itself authorize a
Provider session mutation or prove that the terminal process was removed.

The Provider must clean up terminal resources when their handoff expires, when
the owning sandbox is removed or becomes invalid, and during bounded shutdown
or startup reconciliation. Cleanup must be idempotent and identity-bound so it
cannot remove a foreign or replacement resource.

Configuration must bound at least:

- concurrent terminals per controller and per sandbox;
- terminal lifetime, reconnect attempts, and reconnect backoff;
- WebSocket frame and message sizes, read/write deadlines, and queued bytes;
- broker attach attempts and operation timeouts; and
- shutdown cleanup duration.

An explicit close-session API, resize messages, keepalive semantics beyond
standard WebSocket control frames, and session replacement require separate
protocol authority. They are excluded from this plan unless the relevant
Contract or caller-owned Gateway protocol is reviewed and locked first.

## Delivery Slices

### P2.5f0: authority, data-plane, recovery, and deployment audit

Record the verified gaps, reject one-shot Docker exec attach as restart
evidence, define ownership and persistence boundaries, and split the vertical
into reviewable implementation slices.

Gate: documentation review and `git diff --check`. This is design evidence
only.

### P2.5f1: reconnectable terminal runtime port and Docker adapter

Define provider-neutral allocate, observe, attach, and cleanup contracts. Add a
Docker development adapter backed by a genuinely reconnectable terminal
mechanism. Keep all backend identity in adapter-private durable state.

Gate: focused PTY/stdin/output, context cancellation, identity conflict,
capacity, expiry, cleanup, provider-restart reattach, sandbox-removal, and real
Docker integration tests. Resize remains excluded.

Local result: commit `6778d3c` defines the backend-neutral allocate, observe,
attach, cleanup, receipt, observation, and context-aware stream contracts. Its
Docker development adapter launches a guest broker that owns the PTY and shell;
each attach uses a new Docker exec to connect to the same broker. Container ID,
broker exec ID, and Unix-socket path remain in adapter-private durable state,
while callers receive only `ref:terminal/<opaque>` evidence. Per-sandbox and
per-controller limits, expiry, identity-bound process-group cleanup, lost-start
recovery, and corrupt-state rejection are covered by focused tests.

Full race/shuffle, vet, `go mod verify`, unchanged Contract verification, the
locked 38-case Suite, diff checks, and live integration for `./driver/docker`
and `./provider/lifecycle/driver/docker` passed locally. The live test preserves
a shell variable and the broker exec identity across Provider driver
reconstruction. It builds the broker into the test sandbox's writable
`/workspace`, so fixed read-only image packaging remains a deployment and
production prerequisite. This result is P2.5f1 runtime-adapter evidence only;
P2.5f3-f7, external caller, aggregate conformance, multi-controller,
multi-tenant, deployment, and production gates remain open. Repository CI
`33033284420` passed all three jobs for evidence baseline `66dd3d1` without
expanding that boundary.

### P2.5f2: durable session coordination and repository migration

Extend the session application with accept-before-allocation dispatch,
receipt attachment, observation, reconciliation, and lifecycle authority
synchronization. Migrate v1 session files without losing accepted operations
or opaque handoff evidence.

Gate: idempotent replay/conflict, allocation lost-response, no duplicate
allocation, generation/fencing, restart, outcome-unknown, expiry, corruption,
and lifecycle-authority matrices.

Local result: commit `ccffd52` adds a separate `CoordinationAuthority`, a
single-controller session `Vertical`, and session repository version 2. The
coordinator projects trusted lifecycle ready/revision/generation/lease state
before reservation and completion while maintaining the admitted session fence
as a session-local high-water mark. It durably accepts before allocation, binds
allocation identity to the persisted acceptance time, attaches only an exact
provider-neutral `ref:terminal/*` receipt, observes without replacement,
terminalizes known absence/expiry, preserves uncertain outcomes, and retries
exact-identity cleanup. A later registry may supply `ref:session:*` evidence to
the success hook, but this slice neither creates nor resolves it.

Valid v1 accepted operations remain accepted, legacy successful handoffs remain
readable, and unsupported legacy running records migrate to `outcome_unknown`
instead of risking duplicate allocation. Focused fault/replay/fencing,
restart, expiry/cleanup, corruption, lifecycle, and migration matrices pass.
Full race/shuffle, vet, `go mod verify`, unchanged Contract verification, the
locked 38-case Suite, diff checks, and both live Docker integration packages
also pass locally at `ccffd52`. Post-push CI `33045725476` passed
`provider-contract`, `test`, and `docker-integration` for evidence baseline
`3abefd4`. This is uncomposed single-controller component evidence only; it does
not establish a terminal/Gateway vertical, cross-repository atomicity, external
caller, aggregate conformance, multi-controller, multi-tenant, deployment, or
production readiness.

### P2.5f3: opaque reference registry and resolver

Local and repository CI gates passed in implementation `b1acdd1`, evidence
baseline `7138a4c`, and CI `33059304542`. The separate registry persists a
random 128-bit `ref:session:*` record bound to the running allocation's sandbox,
revision, operation/attempt, fencing, runtime session, profile, generation,
expiry, and provider-neutral `ref:terminal/*` receipt. A resolver rechecks
active registry state and the exact session authority handoff on `Resolve` and
again on every `Dial`, then creates a fresh `terminal.Stream` attach closure
without persisting a Go closure or returning backend IDs, sockets, URLs,
addresses, or credentials.

The file adapter uses versioned JSON, atomic replacement, directory sync, and a
single-controller lock. Reference and session repositories are deliberately
non-atomic: a record that is never committed as session handoff remains
unresolvable. Collision, restart reconstruction, expiry, revocation,
generation/reference mismatch, and concurrent resolve/revoke tests pass
locally. This is component evidence only, not WebSocket/Gateway composition,
external caller E2E, multi-controller, multi-tenant, deployment, or production
readiness evidence.

### P2.5f4: WebSocket and terminal stream adapters

Implementation `14f14cc` selects `github.com/coder/websocket@v1.8.15` (ISC) as
a direct dependency and adds `gateway/adapter`, leaving both WebSocket and
Provider terminal imports outside the Gateway policy package. Source review
confirmed context-aware reads/writes, per-message limits, close handshake, and
ping/pong support. No `govulncheck` or OSV scan was available, so no
vulnerability-scan claim is made.

`NewWebSocketServer` refuses to start without caller-owned handshake admission;
it runs that callback before upgrade, writes only a generic `403` on rejection,
retains default same-origin verification, rejects wildcard origin patterns, and
disables compression. It maps only text/binary messages to `gateway.Stream` and
uses 32 KiB default / 64 KiB maximum inbound and outbound limits. WebSocket
control frames remain library-handled rather than terminal bytes. The terminal
adapter reads bounded binary frames, loops partial writes for text/binary input,
and rejects control frames. Both adapters pass contexts to their data-plane
ports; terminal close is checked before invocation but cannot promise to cancel
an arbitrary blocking `terminal.Stream.Close`.

Focused race/shuffle tests cover admission, origin rejection, binary flow,
ping/pong, normal and abrupt close, inbound `1009`, cancellation, terminal
partial I/O, limits, and idempotent close. Full race/shuffle, vet, module
verification, unchanged Contract verification, the 38-case Suite, diff checks,
and both Docker regression packages passed locally. Evidence baseline `1d9da67`
then passed repository CI `33064864447` (`provider-contract`, `test`, and
`docker-integration`). This is adapter/component evidence only; no authorizer
is supplied.

Gate: bidirectional binary data, control frames, oversized input, slow reader
and writer, abrupt disconnect, cancellation, race, and dependency/security
review. This slice does not provide an authorizer.

### P2.5f5: Gateway composition

Compose the existing Gateway only when a caller-owned `Authorizer`,
`RevocationSource`, and `Recorder`, plus the Provider-backed resolver and both
wire adapters, are supplied. Missing dependencies fail startup or leave the
Gateway disabled; no allow-all fallback is permitted.

Gate: authorization binding, cross-tenant denial, expiry, revocation
interruption, fresh resolution on reconnect, reconnect exhaustion,
metadata-only audit, and failure-closed dependency matrices.

### P2.5f6: command configuration and process lifecycle

Add explicit development configuration, startup recovery order, capacity
limits, and shutdown cleanup. Inject the session application into Provider
transport only after recovery succeeds. Continue to reject the development
terminal/Gateway composition in production mode.

Gate: config validation, disabled/incomplete/enabled startup matrices,
repository locking, startup reconciliation, shutdown ordering, and route
nondisclosure. Capability advertisement remains empty until P2.5h.

Local result: commits `c4c7cbc` and `8a794c0` add an explicit, default-disabled
development terminal configuration with separate file-backed session and opaque
reference stores, fixed guest broker/shell paths, bounded per-sandbox and
per-controller capacity, and bounded shutdown cleanup. Enabled configuration
requires mTLS/JWS protected admission plus the Docker Provider lifecycle and
its file repository; production mode rejects this development composition.
Startup recovers lifecycle, exec, then session/reference state before injecting
the session application into protected Provider transport. The session vertical
reuses the exact previously registered opaque reference when a completion is
replayed or recovered, preventing unbounded unresolvable registrations after a
failed final authority check. Listener shutdown precedes revocation and
identity-bound cleanup of still-running uncommitted allocations; successful,
unexpired handoffs remain durable for provider-process reattachment. No public
Gateway route, caller policy default, Contract/DTO change, or capability
advertisement was added. Config, startup/locking, recovery, shutdown-order,
route-absence regression, full race/shuffle, vet, unchanged Contract
verification, the locked 38-case Suite, and live Docker terminal regressions
pass locally; repository CI is still pending.

### P2.5f7: terminal vertical evidence gate

Run focused race/fault matrices, full race/shuffle, vet, unchanged Contract
verification, the locked 38-case Suite, dependency-boundary checks, and live
Docker integration. Record repository CI separately.

This gate closes only the single-controller development terminal/Gateway
vertical. Independent caller E2E remains P2.5i. Aggregate conformance,
multi-controller reliability, hostile multi-tenant security, deployment, and
production readiness remain open.

## Required Validation

All Go slices require:

```bash
go test -race -shuffle=on -count=1 ./...
go vet ./...
```

Provider transport or Contract projection changes also require:

```bash
go run ./cmd/verify-contract -source-root .
go run ./cmd/run-conformance -source-root . -race -shuffle
```

Docker terminal behavior is exercised by the Provider lifecycle Docker package:

```bash
SANDBOX_RUNTIME_DOCKER_INTEGRATION=1 go test -tags=integration -count=1 ./driver/docker ./provider/lifecycle/driver/docker
```

The second package now builds and runs the terminal broker, reconnects after
Provider driver reconstruction, checks same-shell and broker identity, and
cleans up the terminal resource. Repository CI `33033284420` independently ran
and passed this job for evidence baseline `66dd3d1`.

## Non-goals and Evidence Boundary

P2.5f does not add or prove:

- a new Provider Contract route or aggregate conformance;
- platform user, tenant, grant, or public Gateway authority;
- explicit close-session or resize protocol semantics;
- distributed revocation or multi-controller persistence;
- hostile multi-tenant isolation;
- a deployable production topology; or
- an independently owned caller/platform E2E.

P2.5f0-f5 current local and repository CI gates are complete. P2.5f6 local
command-composition evidence is complete and awaits CI; P2.5f7 remains the
single-controller terminal vertical evidence gate. P2.5g, P2.5h, and P2.5i
remain ordered after the terminal vertical.
